import common

module player_client{
	archive_sync ntf(UserData info);
    battle_victory ntf();

    achievement_complete ntf(UserAchievement achieve, UserWeekAchievement wAchieve);

    rank_reward ntf(RankReward reward);
}
