
module login_player{
    player_login_no_token req(string account) rsp(string token, bool is_new) err(int32 err);
    player_login req(string openid, string anonymous_openid = "") rsp(string token, bool is_new) err(int32 err);
}

